# Worklog

## 2026-06-21 — Issue #25: M2 repo-currentness cleanup (branch `main`)

- **Done:** refreshed the repo-facing M2 docs after PR #24/P6: `M2_BRIEF.md` no longer reports every M2 task as `NOT STARTED`; `M2_TICKETS.md` is marked as the completed M2 execution plan; `M2_KICKOFF_PROMPT.md` is marked historical. Claim limits stay explicit: record-keeping completeness only, no current-compliance, no certification, no production readiness, and no Capability Manifest level above `NONE`.
- **Red / follow-up:** E-006 remains the advisory follow-up: result warnings are implemented, but durable `ADVISORY_OUTPUT` records in PassportView flags are deferred until there is trace-safe advisory emission, an appropriate reason-code/result channel, and reachability-compatible linkage. This is not a Compliance Twin shortcut and advisory material must not enter Compliance materialization without a governed bridge.
- **Next:** run the package self-check and review the doc-only diff before handoff.

## 2026-06-20 — P5: SI evidence floor → package content + advisories (branch `m2/p5-si-evidence-floor`)

- **Done:** moved the SI operation-claim evidence floor out of `kernel/policy.py` constants into package content (`profile_si_ffs/evidence_review_policy_v0_1.json`), read generically by a new loader (`kernel/profile_policy.py`, config-pointed, fail-closed → governed `PROFILE_NOT_ACTIVE`). `kernel/` now carries no SI floor value. Raised authorisation-mismatch (non-EXACT product binding) + dose-range advisories as **non-blocking `WARNING` result problems** computed from the policy's advisory rules — never block, never a consequence/compliance fact, never routed to review.
- **Red / follow-up (durable advisory):** the **durable** Advisory-twin record (`ADVISORY_OUTPUT` surfaced in the passport `_advisory_flags`) is **deferred** (ERRATA E-006). Emitting it inside an operation-claim commit needs a trace-safe advisory-emission mechanism with its own `PromotionTrace` — the reachability invariant (`kernel_require_trace_source`) has no advisory emitted-ref slot, and the registry has no advisory reason-code family. **PROFILE.md §35 is partially implemented** (result warning yes; durable passport record = follow-up ticket: durable Advisory-twin emission + advisory reason-code family + advisory result channel).

## 2026-06-14 — CI: bump GitHub Actions to Node-24-capable versions (branch `ci/node24-readiness`)

- **Done:** ahead of GitHub's **2026-06-16** runner default-to-Node-24 switch, bumped the conformance workflow's action pins to the minimum Node-24-capable majors: `actions/checkout@v4→v5`, `actions/setup-python@v5→v6`, `actions/upload-artifact@v4→v6`. Pins-only change; `python-version: "3.12"`, the upload `name`/`path`, and `runs-on: ubuntu-latest` are untouched (YAML re-parsed clean). Verified by a 5-agent web-research pass against the first-party github.blog changelog + each action's `action.yml`: all three old pins were node20-era; `upload-artifact@v5` is still node20 (so v6 is the floor); `checkout@v6` was **deliberately skipped** (it adds a credential-handling change with reported regressions — v5 is the clean node24 floor). No breaking changes affect this workflow's usage.
- **Red:** none. 2026-06-16 is **not** a hard-fail — node20 actions are force-run under Node 24 with only a deprecation warning; the hard removal of Node 20 is **fall 2026**. This is the recommended-now fix (silences warnings, ready before the fall removal), not an emergency. Did NOT add `ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION` (that is the opt-OUT, the opposite of the goal).
- **Next:** the PR's own CI run exercises the bumped versions end-to-end; merge once green.

## 2026-06-14 — Close review mediums M3 + M6 (branch `m1/extent-and-bridge-test`, PR stacked on #5)

- **Done:** the last two M1-kernel leftovers from the 2026-06-14 review, as a separate PR:
  - **M3 (Rule 4/7) — unquantified / fake-bound partial extent.** A non-whole `executionExtent.extentClass` (PARTIAL_TARGET_SCOPE / FAILED_PASS / RETREATMENT_AREA / DISPUTED_AREA / EXTERNAL_GEOMETRY_REFERENCE) carrying no `area`/`geometryRef`/`extentRef`/`scopeExtentBasisRef` reached PROMOTE_ACCEPTED and materialized as if whole-scope — "size treated" silently lost. New `ExecutionExtentValidator` (in `OPERATION_SEQUENCE`, right after carrier semantics) requires an inline `area` (value+unit) or an extent ref that resolves to a **recognized extent-bound carrier kind** (`policy.M1_ALLOWED_EXTENT_BOUND_KINDS` — **empty in M1**, since there is no geometry/extent ingestion surface, so inline `area` is the only M1 bound; declared in `UNSUPPORTED_SURFACES.md`). `NON_WHOLE_EXTENT_CLASSES` also single-homed in `policy.py`. No bound at all → FAIL_CARRIER / `EVIDENCE_INSUFFICIENT` (**ERRATA E-004** — registry has no extent-completeness code, same class as E-001); a ref bound that is **dangling OR wrong-kind** → FAIL_REFERENCE_RESOLUTION / `EVIDENCE_REFERENCE_UNAVAILABLE`. Both stay RETAIN_DRAFT. (Took the PR #6 reviewer two re-review rounds to land: first the truthiness-only gap, then "resolves to something ≠ resolves to the right kind" — the same lesson as H3/M2.)
  - **M6 (test-integrity, Rule 4) — advisory→compliance bridge had no negative test.** Added conformance `test_08b`: commits an ADVISORY_OUTPUT (RETAIN_DRAFT, no consequence) alongside an accepted spray, then asserts no advisory-derived id appears in the MaterializationBasis or any register row, and every register row resolves to an EXECUTION_CONFIRMED consequence — pinning the "no Advisory material in a Compliance materialization" invariant that previously rested on silent defaults.
  - Verification: suite **49/49** (25 conformance incl. `test_08b` + 8 stage + 16 review-fix regressions in `test_review_fixes.py`, incl. the PR #6 dangling- and wrong-kind extent-ref cases), `ofarm_pkg_contract_check.py` PASS.
- **Red:** nothing new. Remaining review items lean M2 and are deferred by design: M1 (revocation narrowing, fails closed), M4 (`disputeStatus`, latent — CONTEST verbs are M2), M5 (watermark isolation, single-writer pilot).
- **Next:** review/merge of PR #5, then this PR (stacked on `m1/kernel-review-fixes`; retarget to `main` after #5 merges).

## 2026-06-14 — Law-grounded kernel review: four findings fixed (branch `m1/kernel-review-fixes`, PR #5)

- **Done:** a multi-agent review of the M1 kernel (9 dimensions × the seven Kernel rules, per-finding adversarial verification by lens-diverse skeptics, completeness critic — 90 agents) surfaced 0 blockers / 3 high / 6 medium / 9 low / 9 nit; **no live privilege-escalation or silent-allow** — every authority defect fails closed. Fixed the three high + one medium, each pinned by a new regression test (`kernel/tests/test_review_fixes.py`, excluded from the named conformance evidence by nodeid, like the stage tests):
  - **H1 (Rule 6) — AS_OF two-clock collapse.** `store.in_force_consequences(as_of=…)` selected a consequence's *presence* on the payload's `acceptedAt` (app clock) but its *supersession* on `kernel_edge.record_time` (server clock); same-transaction writes guarantee `edge.record_time < acceptedAt`, so an `as_of` in that window dropped the original **and** excluded its correction (a self-contradictory hole at every supersession boundary; opposite skew a duplicate). Both sides now select on the single server-commit axis (`record_time`); `acceptedAt` stays a receipt, never the as-of key. The F6 reconstruction test stays green.
  - **H2 (Rule 4) — UCUM unit gate.** The dose-unit check was `startswith("scheme:ucum")`, admitting bare `scheme:ucum`, an empty code, and substring look-alikes (`scheme:ucumbersome`) — a meaningless unit promoted to an AcceptedEventConsequence. Replaced by single-homed `policy.is_resolved_ucum_unit()` (exact `scheme:ucum:` prefix + non-empty code), used by both the carrier validator and the sufficiency dose-unit floor.
  - **H3 (Rule 4/7) — self-referential evidence.** `_build_assertion` backfilled empty `evidenceRefs` with the captured event id, making the claim its own proof (only OBSERVATION/STRUCTURE could reach it; operation/compliance are guarded by the durable-evidence floor). Backfill removed; the evidence-sufficiency gate now requires every promoting-class submission to carry evidence that **resolves to durable EvidenceRecords** — empty, dangling, **and wrong-kind** refs all refuse (`EVIDENCE_INSUFFICIENT`, RETAIN_DRAFT). The dangling/wrong-kind path (observation/structure skip `ReferenceResolutionValidator`) was caught by the **PR #5 review** — it was the residual half of orig. review finding N6; closing it makes H3 complete — the captured draft is still recorded (capture ≠ commitment).
  - **M2 (Rule 7) — wrong-kind binding ref.** `agronomicIdentityBindingRefs` were resolved by existence only, then dereferenced `b["bindingRole"]`; a ref to any other existing record raised an uncaught `KeyError` → untraced 422/500. Added kind-checked `sufficiency.resolved_bindings()`; `CodeBindingValidator` refuses wrong-kind refs governably (`PRODUCT_BINDING_UNRESOLVED`), and every binding dereference is now crash-safe.
  - Verification: conformance suite re-run after each fix (green every time); final **40/40** (24 conformance + 8 stage + 8 review, incl. the PR #5 dangling/wrong-kind cases), `ofarm_pkg_contract_check.py` PASS, all modules import clean.
- **Red:** the H1 micro-window hole is not deterministically reproducible without clock injection — the new H1 test is a happy-path coherence guard; the fix is correct-by-construction (single axis). Deferred, each its own decision (recorded for the honesty trail): H2 profile-pinned UCUM code allow-list; the `api.py` `except (…, KeyError)` catch-all (M2's root cause is fixed, but the broad catch could still mask unrelated KeyErrors); and the review's remaining medium/low/nit items — notably M3 unbounded partial-extent, M4 latent `disputeStatus` over-claim, M5/L2 single-connection concurrency, M6 missing advisory→compliance bridge negative test, and L4: the 2026-06-12 pride-review entry's "conformance signature byte-identical to the pre-refactor baseline" is unverifiable and non-reproducible by construction (the evidence JSON embeds wall-clock and randomized refs) — read it as "pass/fail parity, manually compared," not a mechanical signature.
- **Next:** review/merge of PR #5 (→ `main`, on top of merged PR #4); fold the deferred medium items into M2 housekeeping.

## 2026-06-12 — Issue #3 elegance refactor (branch `m1/gate-elegance`)

- **Done:** behavior-frozen decomposition of the 1,485-line `gates.py` into `policy.py` (all declarative tables incl. the ticket's four named maps + `FRESHNESS_USE_POLICY`), `stages.py` (typed `GatePass`/`GateRefusal`/`GateReplay` results, one transaction-scoped `GateContext` — D3 preserved by construction), `validators.py` (12 named validation units in law-pinned order), `sufficiency.py` (case builders), `emission.py` (`PromotionEmitter` shared by both promotion flavors, `PromotionTraceWriter`, `ReplayWriter`); `gates.py` is now a 125-line orchestration shell with zero policy branches. New stage-contract tests (7) incl. one that parses the Authority Action Matrix law file and pins the runtime vocabulary to it. Freeze proof: 22/22 conformance suite identical before/after (evidence signature compared), self-check PASS; a 12-agent adversarial equivalence panel compared old vs new line-by-line — 2 real divergences found (logged rationale texts flattened by the shared refusal helper) and restored byte-exact; 4 candidates refuted.
- **Red:** one deliberate, declared divergence kept: `durable_evidence(None)` no longer crashes with an unhandled TypeError (full rollback, no trace) — an explicit `evidenceRefs: null` now produces a governed EVIDENCE_SUFFICIENCY refusal with a stored trace/result; a governed refusal is strictly more lawful than a 500.
- **Next:** PR against main referencing issue #3; M2 work builds on the stage shape.

## 2026-06-12 — PR #2 formal hostile re-review: all four findings implemented (branch `m1/kernel`)

- **Done:** (1) D8 encoded at the queue door — self-acceptance via `/review/accept` is lawful only for routine `OPERATION_CLAIM_ASSERTION` records; a party accepting their own compliance (or any non-operation) assertion refuses with `HUMAN_APPROVAL_REQUIRED`; (2) acceptance is a governed resolution — mandatory reviewer rationale (rides the `ReviewDecision.notes`), optional reviewer evidence (rides `evidenceRefs` + EVIDENCE edges), and routed insufficiencies (actor attribution, product/crop binding, temporal, registry discrepancy) refuse acceptance unless NEW durable evidence is attached; the acceptance sufficiency case evaluates `route-reasons-resolved` against the original commit's recorded problems; (3) the named-actor attribution decision is trace-linked — a second `AUTHORITY_BASIS` edge on the assertion and the attribution result ref on the trace's VALIDATION entry, so both authority decisions are reachable from the promotion path; (4) AS_OF spine guard — with multiple activation/profile/artifact-set records the historical pack/profile context is not reconstructible in M1 and AS_OF refuses (`MATERIALIZATION_INVALID`) instead of silently using the latest. New regression test 93 (5 legs) + test 98z; suite 22/22; self-check PASS.
- **Red:** AS_OF pack/profile context selection by `timeContext` is the M2 path once activation history exists (the guard refuses until then); REJECT/CONTEST queue verbs remain M2.
- **Next:** steward re-review — the reviewer stated they would merge after these four; CI green expected on push.

## 2026-06-12 — PR #2 SECOND hostile review: all seven findings implemented (branch `m1/kernel`)

- **Done:** (1) body-named distinct reviewers never promote — the claim queues and the reviewer accepts under their OWN principal via a `GOVERNANCE_DECISION` commit (`/review/accept`); the acceptance is itself a full gate-chain commit with its own trace, sufficiency case re-evaluated from the captured claim, and consequence promotion; (2) operation actor attribution is governed — a named actor differing from the submitter must hold a live grant/delegation for the operation on the farm, else the claim routes to review (`ACTOR_BINDING_UNRESOLVED`); (3) containment hardened — TENANT/DEPLOYMENT refused on the commit path, missing/non-identity scope refs refuse instead of falling through; (4) AS_OF context is as-of-aware — reference snapshots selected by `effectiveFrom <= asOfTime`, never a future vintage; (5) inactive parties denied on the SharingGrant path too; (6) inverted/unparseable freeze windows refuse with no durable artifact; (7) the reachability trigger now verifies edge/trace-payload agreement (destination listed in the trace's emitted refs; consequences only under `PROMOTE_ACCEPTED`). New regression test 94 (7 legs); delegation negative legs isolated per the reviewer's observation; compliance fixture replays as the honest two-step flow. Suite 20/20; self-check PASS.
- **Red:** review REJECT/CONTEST queue actions are not yet implemented (only acceptance) — the queue's other verbs are M2 with the advisor UI; acceptance-case soft exceptions are resolved by the named reviewer and recorded as `SATISFIED_WITH_EXCEPTIONS` in the gate log.
- **Next:** steward re-review; CI green expected on push.

## 2026-06-12 — PR #2 HOSTILE review: all eight findings implemented (branch `m1/kernel`)

- **Done:** (B1) `/commit` binds the transport principal (`X-Acting-Party`) to `actingPartyRef` — body-level actor spoofing refused; API posture declared in UNSUPPORTED_SURFACES.md (development principal pending OIDC M2, incl. the inline distinct-reviewer limitation); (B2) idempotency digests are always server-computed — forged caller digests can no longer fake a matching replay; (B3) delegations now require a LIVE source-authority path (exists, active, unrevoked, delegator-controlled, covers action + both scopes — no widening, fail closed on missing refs); (B4) farm containment enforced on every scope-bearing payload field (subject, extent target, anchor scopes, compliance subject), not just supersession; (B5) reachability strengthened: a deferred trigger verifies every `PROMOTION_EMITS` source resolves to a stored PromotionTrace — fake-edge attack rejected at commit; (F6) `AS_OF` implemented as real reconstruction (acceptance + supersession-edge record times), surviving later corrections; (F7) `partyState != ACTIVE` fails closed before grant evaluation; (F8) stale reuse reason text says STALE. New hostile regression test 95 (8 legs); suite 19/19; self-check PASS.
- **Red:** two of the eight (B2 forged digest, B5 overstated DB claim) were defects in my own earlier fixes/claims — recorded here for the honesty trail; the M1 API remains a declared development surface until OIDC (M2).
- **Next:** steward re-review under the hostile lens; CI green on push expected.

## 2026-06-12 — PR #2 steward review: all five findings implemented (branch `m1/kernel`)

- **Done:** (1) authority vocabulary moved to the accepted Action Matrix — commitClass→action-class map at the gate (`ASSERT_OPERATION_CLAIM`, `ASSERT_COMPLIANCE`, `OBSERVE_*`, `OUTPUT_APPROVE/FILE_*`, `RECEIVE_READ_DATA`, `REVIEW_ACCEPT`), demo grants/manifest/tests updated, no parallel dialect remains; (2) every `identityBasisRefs` member dependency-indexed (extent target + carrier anchor identities incl. crop cycles) with explicit partial-batch broadening per RFC §6.5; (3) compliance assertions now require a structured claim (statement, asserted status, recognized governing rules, resolvable subject) validated at the gate AND evaluated by the sufficiency case — thin claims refuse, self-review still routes to the advisor; (4) `requiredFreshness` modes implemented as distinct semantics with high-consequence escalation; (5) real cross-farm supersession regression (farm-B truth attacked from farm A → `SCOPE_NOT_AUTHORIZED`, no edge) + GitHub Actions CI (`.github/workflows/conformance.yml`: self-check + suite on postgres service, evidence artifact upload). New tests 96 + extended 97; suite 18/18; self-check PASS.
- **Red:** ADVISORY_OUTPUT has no Action Matrix class — mapped to `OBSERVE_CREATE_OBSERVATION` (least-authority, never promotes); candidate for the post-pilot matrix amendment if the steward wants a dedicated class.
- **Next:** steward re-review of PR #2; first CI run on push validates the workflow.

## 2026-06-12 — Steward review 4489049376 of PR #4: all five findings closed (branch `m1/gate-elegance`)

- **Done:** verified all 5 findings real (adversarial agent panel + first-hand), then fixed all 5. (1) `ComplianceClaimValidator` now refuses a `subjectScopeRef` resolving to any non-IdentityRecord and applies farm containment unconditionally (was: silent pass-through). (2) `resolve_for_use` derives `invalidationTriggerFamilies` from the stored InvalidationEvaluationTrace rows per materialization key (strictly newer than the mat's `generated_at`), mapped to the result contract's closed enum — never the hard-coded `TRUTH_BASIS` (the old defending comment was false: the runtime's own API stales via REFERENCE/IDENTITY/CONTEXT families and records them). (3) The `complianceClaim:` notes-tunnel is gone: the structured claim is a durable `ofarm.complianceclaim.v0.1` carrier record (NEW_CANDIDATE contract per Constitution §6.16) linked by a new `COMPLIANCE_CLAIM` edge; recovery is edge-based and fails closed; envelope notes stays the farmer's narrative. (4) `NO_CURRENT_STATE_DEPENDENCY` narrowing declared in UNSUPPORTED_SURFACES.md + ERRATA **E-003** (CONTRACT_GAP: the mode is an undescribed enum value defined nowhere in `reference/`; the result contract admits no lawful no-basis outcome) — the reviewer's literal "true no-materialization path" was **refused** as a law conflict (it cannot produce a contract-valid honest result and would weaken test_96); the mode's intent is already honored at the QueryPlanIR step layer. (5) "literal middleware" reworded to "transaction-scoped orchestration chain" in both `kernel/gates.py` and `PLATFORM.md` (the law never says middleware). Bonus hardening from our own verification panel: type-confused `assertedStatus`/`governingRuleRefs` are now governed refusals, never a 422/500 crash. All additive tests: conformance legs 97(a3)/(a4), 95-F8 + 96 family assertions, 96 no-basis refusal leg; 4 new stage tests. Suite 32/32; self-check PASS; pyflakes clean; manifest artifacts regenerated (registry-derived `supportedArtifactTypes` includes the new candidate kind); 3-agent adversarial verification of the diff found zero blockers.
- **Red:** nothing red. Note for operators: `schema.sql` now refreshes the `kernel_edge` CHECK idempotently (pre-existing databases pick up `COMPLIANCE_CLAIM` on next migrate; the dev `.pgrun` database was migrated and verified).
- **Next:** steward re-review of PR #4; merge closes issue #3. Post-pilot amendment candidates now on register: E-003 (define `NO_CURRENT_STATE_DEPENDENCY` or add a no-basis result outcome).

## 2026-06-12 — Pride review of the elegance refactor (branch `m1/gate-elegance`)

- **Done:** a 57-agent self-review of the refactored kernel (5 lenses: correctness, cycles/coupling, naming/comment truth, dead code, doc drift) triaged 52 findings → 45 fixed / 6 deferred / 1 refuted. Real bug fixed: an unknown carrier `schemaVersion` raised `UnknownContract` past the validator's `except ContractViolation` as an uncaught 500 — now a governed FAIL_SCHEMA refusal, pinned by a new stage test. Structural: stages↔emission import cycle broken (TYPE_CHECKING + one-directional runtime edge), `EnvelopePersist` is a chain stage (no more `isinstance` sniffing in the shell), acceptance target fetched once onto the context, authority-decision triple-insert centralized, `mint()`/`RUNTIME_VERSION`/snapshot+policy refs single-homed in `config.py`/`policy.py`, dead `problems.GateRefusal` deleted, API edge refusals now full `RuntimeProblem`s, conformance evidence file now carries conformance results only (stage tests excluded by nodeid), README/PROFILE.md/schema.sql comment drift corrected (sufficiency cases: three generation points, not two). Suite 30/30 (22 conformance + 8 stage), conformance signature byte-identical to the pre-refactor baseline, self-check PASS, pyflakes clean.
- **Red:** nothing new red. Six findings deferred as accepted posture, recorded here for honesty: (1) `GateContext` stays a wide bag shared by all stages; (2) validators import `GatePass` from stages while stages imports validators' gate — acceptable one-way layering, not a cycle; (3) `_store_case` boolean keyword instead of two named methods; (4) review-route reasons remain stringly `dict`s rather than a typed class; (5) `find_by_kind(...)[-1]` latest-instance convention documented, not enforced; (6) demo grant wording duplicates matrix prose. None changes behavior; all are candidates for M2 housekeeping.
- **Next:** steward review of PR #4; merge closes issue #3.

## 2026-06-12 — Preflight-review cleanup (branch `m1/kernel`, commit 1c623c3)

- **Done:** investigated the external preflight review's five findings against the M1 kernel — implementation verified unaffected (zero FITO references in `kernel/`/`views/`; binding path already REGSR + decision-number identity per D9); landed the minimum controlled patch set: ERRATA E-002 (stale FITO-INFO wording), five doc locations re-worded to SI:UVHVVR-FFS-REG/REGSR, PROFILE.md shipped/deferred status corrected (nothing deferred remains), CONTRACTS_MANIFEST profileInstances 3→9 to match the checker, README counts + Slovenia-local cut-date currentness note.
- **Red:** nothing new; the two open items from the M1 entry below stand.
- **Next:** PR #2 review/merge; then M2 per the M1 entry below.

## 2026-06-12 — M1 Kernel build (branch `m1/kernel`)

- **Done:** full M1 brief tasks 1–7 — PostgreSQL append-only truth store + gate pipeline + materializer with the explainable-evidence draft shapes (D16, draft lane only), two view artifact pairs + output generator with refusal behavior, Capability Manifest (level `NONE`, no over-claim), conformance tests 1–15 with the 8 gate-sequencing fixtures replayed live (17/17 green, executed evidence in `conformance/evidence/`), package self-check PASS; multi-agent adversarial review confirmed 44 findings (3 blockers: D9 locator-as-identity re-verification, unvalidated cross-farm supersession, authz-result contract crash) — all fixed and pinned by regression test 97; ERRATA E-001 filed (registry has no temporal-conformance reason code).
- **Red:** freshness-vector watermarks are not snapshot-isolated under concurrent multi-process writers (accepted for the single-writer pilot; revisit at M2); steward question: the pre-existing M0 parse `profile_si_ffs/examples/regsr_snapshot_2026-06-12.json` carries sole-proprietor business names in public-register representative fields — confirm the D14 posture (file untouched: it is provenance evidence).
- **Next:** M2 — registry adapter scheduling (reuse `tooling/regsr_snapshot/`, fetch detail pages for bound products so D9 identity re-verification is confirmable instead of review-routed), GERK importer, OIDC onto Party/RoleAssignment; benchmark evidence per the explainable-evidence RFC before any capability claim above `NONE`.
