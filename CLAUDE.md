@AGENTS.md

Start here: `AGENTS.md` (working rules — privacy rule 1 is absolute), then `DECISIONS.md`, then `M1_BRIEF.md` for the current work order. Run `python3 conformance/ofarm_pkg_contract_check.py` before every commit.
