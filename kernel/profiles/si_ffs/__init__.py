"""SI FFS pilot profile adapters (package content). REGSR / GERK /
FFSNaprave specifics live here, riding the generic G2 import + G3 verification
mechanisms; they never leak into kernel/adapters.py or kernel/verification.py."""
