"""Installed-profile adapter code (M2 Phase 2). Each subpackage is one
profile's package content riding the generic Core/Platform mechanisms
(kernel/adapters.py, kernel/verification.py) — scheme specifics live here,
never in the generic kernel."""
