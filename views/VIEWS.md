# Static Views

The SI FFS view specifications and authored QuerySpecification/QueryPlanIR artifacts now live in `../profile_si_ffs/views/`.

This pointer is retained so legacy references to `views/VIEWS.md` keep resolving. The SI view artifacts are profile/package material, not universal OFARM Core or Platform law.
